# Paper Reading 配置文件
# 复制此文件为 .env 并修改对应参数
# 所有参数均为可选，不设置则使用默认值

# 数据根目录（数据库、原始文献、分类均在此目录下）
# 默认：项目根目录
# 跨设备：设为 OneDrive/Dropbox 等云同步文件夹路径
# PAPERS_HOME=D:/OneDrive/PaperReading

# 数据库文件路径（优先级高于 PAPERS_HOME）
# PAPERS_DB_PATH=D:/MyPapers/papers.db

# 原始文献目录（PDF + notes）
# PAPERS_DIR=D:/MyPapers/原始文献

# 数据库模式：shared（共享，所有人看同一批论文）或 per_user（每人独立数据库）
# PAPERS_DB_MODE=shared

# 服务器端口
# PAPERS_PORT=3000
